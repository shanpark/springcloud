<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="iotree.service.mapper.SampleMapper" >

    <!--
    <select id="getTopicCount" resultType="Integer" databaseId="mariadb">
        SELECT COUNT(*)
        FROM Topic
        WHERE projectUid = #{projectUid}
    </select>

    <select id="getTopicList" resultType="iotree.service.vo.TopicVo" databaseId="mariadb">
        SELECT topic, description, created
        FROM Topic
        WHERE projectUid = #{projectUid}
        ORDER BY created DESC
        LIMIT #{from}, #{count}
    </select>

    <insert id="createTopic" databaseId="mariadb">
        INSERT INTO Topic
            (projectUid, topic, description)
        VALUES
            (#{projectUid}, #{topic}, #{description})
    </insert>

    <update id="updateTopic" databaseId="mariadb">
        UPDATE Topic
        SET description = #{description}
        WHERE topic = #{topic}
    </update>

    <delete id="deleteTopic" databaseId="mariadb">
        DELETE FROM Topic
        WHERE projectUid = #{projectUid} AND topic = #{topic}
    </delete>
    -->

</mapper>
