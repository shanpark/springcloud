package iotree.service.mapper;

import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface SampleMapper {
    // int getTopicCount(Long projectUid);
    // List<TopicVo> getTopicList(Long projectUid, int from, int count);

    // int createTopic(TopicVo topicVo);
    // int updateTopic(TopicVo topicVo);
    // int deleteTopic(Long projectUid, String topic);
}
